galaxy_info:
  author: Derek Merck
  description: Configure and run a [Splunk](http://www.splunk.com) data index in a Docker container.

  role_name: splunk-docker

  license: MIT

  min_ansible_version: 2.4

  galaxy_tags:
  - docker
  - splunk

dependencies:
  - role: geerlingguy.pip
    pip_install_packages: docker
    tags: deps
  - role: geerlingguy.docker
    tags: deps
